import { Page } from "./components/Page";
import React from 'react';

/**
 * Компонент, представляющий приложение
 * @returns 
 */
const App = (): React.ReactElement => <Page />;

export default App;
